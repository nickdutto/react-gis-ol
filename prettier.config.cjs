/** @type {import("prettier").Config} */
const config = {
  trailingComma: 'all',
  tabWidth: 2,
  printWidth: 100,
  semi: true,
  singleQuote: true,
  endOfLine: 'auto',
};

// eslint-disable-next-line no-undef
module.exports = config;